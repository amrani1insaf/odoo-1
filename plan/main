#include <turnkey/base>
#include <turnkey/lapp>

adduser
lsb-base
node-less
postgresql-client
python3-babel
python3-pil
python              
python3-dateutil     /* Dateutil : provides powerful extensions to the standard datetime module, available in Python 2.3+. */
python3-decorator    
python3-docutils     
python3-feedparser   /* universal Feed Parser for Python */
python3-html2text
python-imaging      /* provides image processing functionality and supports many file formats. */
python3-jinja2       
python-ldap         /* LDAP interface module */
python-libxslt1     /* python bindings for XSLT transformation library */
python3-lxml         /* lxml is the most feature-rich and easy-to-use library for working with XML and HTML in the Python language. */
python3-mako         /* fast and lightweight templating for the Python platform. */
python3-mock         
python-openid       /* OpenID authentication support for servers and consumers */
python3-passlib      
python3-psutil       
python3-psycopg2     /* Odoo says - the most popular PostgreSQL adapter for the Python programming language */
python-pybabel      /* tools for internationalizing Python applications */
python-pychart      /* library for creating high quality Encapsulated Postscript, PDF, PNG, or SVG charts. */
python3-pydot        /* provides a full interface to create, handle, modify and process graphs in Graphviz's dot language. */
python3-pyparsing    /* library for parsing Python code */
python3-pypdf        
python3-reportlab    /* The ReportLab Toolkit is engine for programmatically creating PDF documents and forms the foundation of RML. */
python3-requests     
python-simplejson   /* simple, fast, extensible JSON encoder/decoder */
python3-suds
python3-tz           /* World Timezone Definitions for Python */
python3-xlsxwriter
python-unittest2    
python3-vatnumber    /* module to validate VAT numbers for European countries */
python-vobject      /* VObject simplifies the process of parsing and creating iCalendar and vCard objects. */
python3-werkzeug     /* collection of utilities for WSGI applications */
python-xlwt         /* module for reading/writing Microsoft Excel spreadsheet files */
python3-yaml         
python-gevent       /* gevent is a coroutine-based Python networking library */

wkhtmltopdf         /* Command line utilities to convert html to pdf or image using WebKit  */
python-gdata        /* Google Data Python client library */

python-pip          /* Needed to install psycogreen */

xfonts-base         /* Needed for upstream wkhtmltox package */
xfonts-75dpi        /* Needed for upstream wkhtmltox package */

